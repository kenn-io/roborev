/**
 * Generated by Orval. Do not edit manually.
 */
import type {
  ListBranchesOutputBody,
  ListBranchesParams,
  ListReposOutputBody,
  ListReposParams,
  RegisterRepoRequest,
  Repo,
  ResolveRepoOutputBody,
  ResolveRepoParams,
} from "../models";

import { roborevFetch } from "../../generated-fetch";

// https://stackoverflow.com/questions/49579094/typescript-conditional-types-filter-out-readonly-properties-pick-only-requir/49579497#49579497
type IfEquals<X, Y, A = X, B = never> =
  (<T>() => T extends X ? 1 : 2) extends <T>() => T extends Y ? 1 : 2 ? A : B;

type WritableKeys<T> = {
  [P in keyof T]-?: IfEquals<
    { [Q in P]: T[P] },
    { -readonly [Q in P]: T[P] },
    P
  >;
}[keyof T];

type UnionToIntersection<U> = (U extends any ? (k: U) => void : never) extends (
  k: infer I,
) => void
  ? I
  : never;
type DistributeReadOnlyOverUnions<T> = T extends any ? NonReadonly<T> : never;

type Writable<T> = Pick<T, WritableKeys<T>>;
type NonReadonly<T> = [T] extends [UnionToIntersection<T>]
  ? {
      [P in keyof Writable<T>]: T[P] extends object
        ? NonReadonly<NonNullable<T[P]>>
        : T[P];
    }
  : DistributeReadOnlyOverUnions<T>;

export const getListBranchesUrl = (params?: ListBranchesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/branches?${stringifiedParams}`
    : `/api/branches`;
};

/**
 * @summary List branches with job counts
 */
export const listBranches = async (
  params?: ListBranchesParams,
  options?: RequestInit,
): Promise<ListBranchesOutputBody> => {
  return roborevFetch<ListBranchesOutputBody>(getListBranchesUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getListReposUrl = (params?: ListReposParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/repos?${stringifiedParams}`
    : `/api/repos`;
};

/**
 * @summary List repos with job counts
 */
export const listRepos = async (
  params?: ListReposParams,
  options?: RequestInit,
): Promise<ListReposOutputBody> => {
  return roborevFetch<ListReposOutputBody>(getListReposUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getRegisterRepoUrl = () => {
  return `/api/repos/register`;
};

/**
 * @summary Register a repository
 */
export const registerRepo = async (
  registerRepoRequest: NonReadonly<RegisterRepoRequest>,
  options?: RequestInit,
): Promise<Repo> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<Repo>(getRegisterRepoUrl(), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(registerRepoRequest),
  });
};

export const getResolveRepoUrl = (params?: ResolveRepoParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/repos/resolve?${stringifiedParams}`
    : `/api/repos/resolve`;
};

/**
 * @summary Resolve whether a path belongs to a tracked repo
 */
export const resolveRepo = async (
  params?: ResolveRepoParams,
  options?: RequestInit,
): Promise<ResolveRepoOutputBody> => {
  return roborevFetch<ResolveRepoOutputBody>(getResolveRepoUrl(params), {
    ...options,
    method: "GET",
  });
};
