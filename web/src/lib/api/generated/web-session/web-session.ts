/**
 * Generated by Orval. Do not edit manually.
 */
import type {
  WebBootstrapInputBody,
  WebLoginRequest,
  WebSessionCredentials,
  WebSessionStatus,
} from "../models";

import { roborevFetch } from "../../generated-fetch";

// https://stackoverflow.com/questions/49579094/typescript-conditional-types-filter-out-readonly-properties-pick-only-requir/49579497#49579497
type IfEquals<X, Y, A = X, B = never> =
  (<T>() => T extends X ? 1 : 2) extends <T>() => T extends Y ? 1 : 2 ? A : B;

type WritableKeys<T> = {
  [P in keyof T]-?: IfEquals<
    { [Q in P]: T[P] },
    { -readonly [Q in P]: T[P] },
    P
  >;
}[keyof T];

type UnionToIntersection<U> = (U extends any ? (k: U) => void : never) extends (
  k: infer I,
) => void
  ? I
  : never;
type DistributeReadOnlyOverUnions<T> = T extends any ? NonReadonly<T> : never;

type Writable<T> = Pick<T, WritableKeys<T>>;
type NonReadonly<T> = [T] extends [UnionToIntersection<T>]
  ? {
      [P in keyof Writable<T>]: T[P] extends object
        ? NonReadonly<NonNullable<T[P]>>
        : T[P];
    }
  : DistributeReadOnlyOverUnions<T>;

export const getLogoutWebSessionUrl = () => {
  return `/api/ui/session`;
};

/**
 * @summary Invalidate a browser session
 */
export const logoutWebSession = async (
  options?: RequestInit,
): Promise<void> => {
  return roborevFetch<void>(getLogoutWebSessionUrl(), {
    ...options,
    method: "DELETE",
  });
};

export const getGetWebSessionStatusUrl = () => {
  return `/api/ui/session`;
};

/**
 * @summary Get browser authentication status
 */
export const getWebSessionStatus = async (
  options?: RequestInit,
): Promise<WebSessionStatus> => {
  return roborevFetch<WebSessionStatus>(getGetWebSessionStatusUrl(), {
    ...options,
    method: "GET",
  });
};

export const getBootstrapWebSessionUrl = () => {
  return `/api/ui/session/bootstrap`;
};

/**
 * @summary Mint tab credentials from an ambient browser session
 */
export const bootstrapWebSession = async (
  webBootstrapInputBody: NonReadonly<WebBootstrapInputBody>,
  options?: RequestInit,
): Promise<WebSessionCredentials> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<WebSessionCredentials>(getBootstrapWebSessionUrl(), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(webBootstrapInputBody),
  });
};

export const getLoginWebSessionUrl = () => {
  return `/api/ui/session/login`;
};

/**
 * @summary Exchange a daemon token for a browser session
 */
export const loginWebSession = async (
  webLoginRequest: NonReadonly<WebLoginRequest>,
  options?: RequestInit,
): Promise<WebSessionCredentials> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<WebSessionCredentials>(getLoginWebSessionUrl(), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(webLoginRequest),
  });
};
