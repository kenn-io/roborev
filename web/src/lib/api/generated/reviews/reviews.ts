/**
 * Generated by Orval. Do not edit manually.
 */
import type {
  CloseReviewOutputBody,
  CloseReviewRequest,
  ExportCICostDocument,
  ExportCIMetricsDocument,
  ExportCiCostsParams,
  ExportCiMetricsParams,
  ExportReviewsDocument,
  ExportReviewsParams,
  GetReviewParams,
  Review,
} from "../models";

import { roborevFetch } from "../../generated-fetch";

// https://stackoverflow.com/questions/49579094/typescript-conditional-types-filter-out-readonly-properties-pick-only-requir/49579497#49579497
type IfEquals<X, Y, A = X, B = never> =
  (<T>() => T extends X ? 1 : 2) extends <T>() => T extends Y ? 1 : 2 ? A : B;

type WritableKeys<T> = {
  [P in keyof T]-?: IfEquals<
    { [Q in P]: T[P] },
    { -readonly [Q in P]: T[P] },
    P
  >;
}[keyof T];

type UnionToIntersection<U> = (U extends any ? (k: U) => void : never) extends (
  k: infer I,
) => void
  ? I
  : never;
type DistributeReadOnlyOverUnions<T> = T extends any ? NonReadonly<T> : never;

type Writable<T> = Pick<T, WritableKeys<T>>;
type NonReadonly<T> = [T] extends [UnionToIntersection<T>]
  ? {
      [P in keyof Writable<T>]: T[P] extends object
        ? NonReadonly<NonNullable<T[P]>>
        : T[P];
    }
  : DistributeReadOnlyOverUnions<T>;

export const getExportCiCostsUrl = (params?: ExportCiCostsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/export/ci-costs?${stringifiedParams}`
    : `/api/export/ci-costs`;
};

/**
 * @summary Export job-level CI costs
 */
export const exportCiCosts = async (
  params?: ExportCiCostsParams,
  options?: RequestInit,
): Promise<ExportCICostDocument> => {
  return roborevFetch<ExportCICostDocument>(getExportCiCostsUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getExportCiMetricsUrl = (params?: ExportCiMetricsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/export/ci-metrics?${stringifiedParams}`
    : `/api/export/ci-metrics`;
};

/**
 * @summary Export finalized CI panel metrics
 */
export const exportCiMetrics = async (
  params?: ExportCiMetricsParams,
  options?: RequestInit,
): Promise<ExportCIMetricsDocument> => {
  return roborevFetch<ExportCIMetricsDocument>(getExportCiMetricsUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getExportReviewsUrl = (params?: ExportReviewsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/export/reviews?${stringifiedParams}`
    : `/api/export/reviews`;
};

/**
 * @summary Export completed reviews
 */
export const exportReviews = async (
  params?: ExportReviewsParams,
  options?: RequestInit,
): Promise<ExportReviewsDocument> => {
  return roborevFetch<ExportReviewsDocument>(getExportReviewsUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getGetReviewUrl = (params?: GetReviewParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/review?${stringifiedParams}`
    : `/api/review`;
};

/**
 * @summary Get a review by job ID or SHA
 */
export const getReview = async (
  params?: GetReviewParams,
  options?: RequestInit,
): Promise<Review> => {
  return roborevFetch<Review>(getGetReviewUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getCloseReviewUrl = () => {
  return `/api/review/close`;
};

/**
 * @summary Close or reopen a review
 */
export const closeReview = async (
  closeReviewRequest: NonReadonly<CloseReviewRequest>,
  options?: RequestInit,
): Promise<CloseReviewOutputBody> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<CloseReviewOutputBody>(getCloseReviewUrl(), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(closeReviewRequest),
  });
};
