/**
 * Generated by Orval. Do not edit manually.
 */
import type {
  AnalyticsSnapshot,
  GetReviewProjectionParams,
  GetWebAnalyticsParams,
  ReviewProjection,
} from "../models";

import { roborevFetch } from "../../generated-fetch";

export const getGetWebAnalyticsUrl = (params?: GetWebAnalyticsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/ui/analytics?${stringifiedParams}`
    : `/api/ui/analytics`;
};

/**
 * @summary Get a coherent SQLite analytics snapshot
 */
export const getWebAnalytics = async (
  params?: GetWebAnalyticsParams,
  options?: RequestInit,
): Promise<AnalyticsSnapshot> => {
  return roborevFetch<AnalyticsSnapshot>(getGetWebAnalyticsUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getGetReviewProjectionUrl = (
  params?: GetReviewProjectionParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/ui/review-projection?${stringifiedParams}`
    : `/api/ui/review-projection`;
};

/**
 * @summary Get a versioned read-only review projection
 */
export const getReviewProjection = async (
  params?: GetReviewProjectionParams,
  options?: RequestInit,
): Promise<ReviewProjection> => {
  return roborevFetch<ReviewProjection>(getGetReviewProjectionUrl(params), {
    ...options,
    method: "GET",
  });
};
