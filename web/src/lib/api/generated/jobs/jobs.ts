/**
 * Generated by Orval. Do not edit manually.
 */
import type {
  BackfillTokensRequest,
  BatchJobsOutputBody,
  BatchJobsRequest,
  CancelJobOutputBody,
  CancelJobRequest,
  EnqueueCreatedResponse,
  EnqueueRequest,
  EnqueueSkippedResponse,
  FixJobRequest,
  GetJobLogParams,
  GetJobOutputParams,
  GetJobPatchParams,
  JobIDRequest,
  JobStatusOutputBody,
  ListJobsOutputBody,
  ListJobsParams,
  PanelEnqueueResponse,
  RemapRequest,
  RemapResult,
  RerunJobOutputBody,
  RerunJobRequest,
  ReviewJob,
  TokenSummary,
  UpdateJobBranchOutputBody,
  UpdateJobBranchRequest,
} from "../models";

import { roborevFetch } from "../../generated-fetch";

// https://stackoverflow.com/questions/49579094/typescript-conditional-types-filter-out-readonly-properties-pick-only-requir/49579497#49579497
type IfEquals<X, Y, A = X, B = never> =
  (<T>() => T extends X ? 1 : 2) extends <T>() => T extends Y ? 1 : 2 ? A : B;

type WritableKeys<T> = {
  [P in keyof T]-?: IfEquals<
    { [Q in P]: T[P] },
    { -readonly [Q in P]: T[P] },
    P
  >;
}[keyof T];

type UnionToIntersection<U> = (U extends any ? (k: U) => void : never) extends (
  k: infer I,
) => void
  ? I
  : never;
type DistributeReadOnlyOverUnions<T> = T extends any ? NonReadonly<T> : never;

type Writable<T> = Pick<T, WritableKeys<T>>;
type NonReadonly<T> = [T] extends [UnionToIntersection<T>]
  ? {
      [P in keyof Writable<T>]: T[P] extends object
        ? NonReadonly<NonNullable<T[P]>>
        : T[P];
    }
  : DistributeReadOnlyOverUnions<T>;

export const getEnqueueJobUrl = () => {
  return `/api/enqueue`;
};

/**
 * @summary Enqueue a daemon job
 */
export const enqueueJob = async (
  enqueueRequest: NonReadonly<EnqueueRequest>,
  options?: RequestInit,
): Promise<
  EnqueueCreatedResponse | PanelEnqueueResponse | EnqueueSkippedResponse
> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<
    EnqueueCreatedResponse | PanelEnqueueResponse | EnqueueSkippedResponse
  >(getEnqueueJobUrl(), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(enqueueRequest),
  });
};

export const getMarkJobAppliedUrl = () => {
  return `/api/job/applied`;
};

/**
 * @summary Mark a fix job as applied
 */
export const markJobApplied = async (
  jobIDRequest: NonReadonly<JobIDRequest>,
  options?: RequestInit,
): Promise<JobStatusOutputBody> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<JobStatusOutputBody>(getMarkJobAppliedUrl(), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(jobIDRequest),
  });
};

export const getCancelJobUrl = () => {
  return `/api/job/cancel`;
};

/**
 * @summary Cancel a queued or running job
 */
export const cancelJob = async (
  cancelJobRequest: NonReadonly<CancelJobRequest>,
  options?: RequestInit,
): Promise<CancelJobOutputBody> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<CancelJobOutputBody>(getCancelJobUrl(), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(cancelJobRequest),
  });
};

export const getCreateFixJobUrl = () => {
  return `/api/job/fix`;
};

/**
 * @summary Create a background fix job
 */
export const createFixJob = async (
  fixJobRequest: NonReadonly<FixJobRequest>,
  options?: RequestInit,
): Promise<ReviewJob> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<ReviewJob>(getCreateFixJobUrl(), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(fixJobRequest),
  });
};

export const getGetJobLogUrl = (params?: GetJobLogParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/job/log?${stringifiedParams}`
    : `/api/job/log`;
};

/**
 * @summary Get raw job log bytes
 */
export const getJobLog = async (
  params?: GetJobLogParams,
  options?: RequestInit,
): Promise<void> => {
  return roborevFetch<void>(getGetJobLogUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getGetJobOutputUrl = (params?: GetJobOutputParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/job/output?${stringifiedParams}`
    : `/api/job/output`;
};

/**
 * @summary Get or stream in-memory job output
 */
export const getJobOutput = async (
  params?: GetJobOutputParams,
  options?: RequestInit,
): Promise<void> => {
  return roborevFetch<void>(getGetJobOutputUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getGetJobPatchUrl = (params?: GetJobPatchParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/job/patch?${stringifiedParams}`
    : `/api/job/patch`;
};

/**
 * @summary Get a stored fix patch
 */
export const getJobPatch = async (
  params?: GetJobPatchParams,
  options?: RequestInit,
): Promise<void> => {
  return roborevFetch<void>(getGetJobPatchUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getMarkJobRebasedUrl = () => {
  return `/api/job/rebased`;
};

/**
 * @summary Mark a fix job as rebased
 */
export const markJobRebased = async (
  jobIDRequest: NonReadonly<JobIDRequest>,
  options?: RequestInit,
): Promise<JobStatusOutputBody> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<JobStatusOutputBody>(getMarkJobRebasedUrl(), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(jobIDRequest),
  });
};

export const getRerunJobUrl = () => {
  return `/api/job/rerun`;
};

/**
 * @summary Re-enqueue a completed or failed job
 */
export const rerunJob = async (
  rerunJobRequest: NonReadonly<RerunJobRequest>,
  options?: RequestInit,
): Promise<RerunJobOutputBody> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<RerunJobOutputBody>(getRerunJobUrl(), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(rerunJobRequest),
  });
};

export const getUpdateJobBranchUrl = () => {
  return `/api/job/update-branch`;
};

/**
 * @summary Update a job branch
 */
export const updateJobBranch = async (
  updateJobBranchRequest: NonReadonly<UpdateJobBranchRequest>,
  options?: RequestInit,
): Promise<UpdateJobBranchOutputBody> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<UpdateJobBranchOutputBody>(getUpdateJobBranchUrl(), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(updateJobBranchRequest),
  });
};

export const getListJobsUrl = (params?: ListJobsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/jobs?${stringifiedParams}`
    : `/api/jobs`;
};

/**
 * @summary List review jobs
 */
export const listJobs = async (
  params?: ListJobsParams,
  options?: RequestInit,
): Promise<ListJobsOutputBody> => {
  return roborevFetch<ListJobsOutputBody>(getListJobsUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getBatchJobsUrl = () => {
  return `/api/jobs/batch`;
};

/**
 * @summary Fetch jobs with reviews by ID
 */
export const batchJobs = async (
  batchJobsRequest: NonReadonly<BatchJobsRequest>,
  options?: RequestInit,
): Promise<BatchJobsOutputBody> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<BatchJobsOutputBody>(getBatchJobsUrl(), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(batchJobsRequest),
  });
};

export const getRemapJobsUrl = () => {
  return `/api/remap`;
};

/**
 * @summary Remap jobs after git history rewrite
 */
export const remapJobs = async (
  remapRequest: NonReadonly<RemapRequest>,
  options?: RequestInit,
): Promise<RemapResult> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<RemapResult>(getRemapJobsUrl(), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(remapRequest),
  });
};

export const getBackfillTokensUrl = () => {
  return `/api/tokens/backfill`;
};

/**
 * @summary Backfill token usage from AgentsView payloads
 */
export const backfillTokens = async (
  backfillTokensRequest: NonReadonly<BackfillTokensRequest>,
  options?: RequestInit,
): Promise<TokenSummary> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<TokenSummary>(getBackfillTokensUrl(), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(backfillTokensRequest),
  });
};
