/**
 * Generated by Orval. Do not edit manually.
 */
import type { SyncNowParams, SyncStatusOutputBody } from "../models";

import { roborevFetch } from "../../generated-fetch";

export const getSyncNowUrl = (params?: SyncNowParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/sync/now?${stringifiedParams}`
    : `/api/sync/now`;
};

/**
 * @summary Run an immediate sync
 */
export const syncNow = async (
  params?: SyncNowParams,
  options?: RequestInit,
): Promise<void> => {
  return roborevFetch<void>(getSyncNowUrl(params), {
    ...options,
    method: "POST",
  });
};

export const getGetSyncStatusUrl = () => {
  return `/api/sync/status`;
};

/**
 * @summary Get sync worker status
 */
export const getSyncStatus = async (
  options?: RequestInit,
): Promise<SyncStatusOutputBody> => {
  return roborevFetch<SyncStatusOutputBody>(getGetSyncStatusUrl(), {
    ...options,
    method: "GET",
  });
};
