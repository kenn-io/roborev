/**
 * Generated by Orval. Do not edit manually.
 */
import type {
  AgentHookFixDoneOutputBody,
  AgentHookFixDoneRequest,
  AgentHookResetOutputBody,
  AgentHookResetRequest,
  AgentHookResponse,
  AgentHookSessionsOutputBody,
  Request,
} from "../models";

import { roborevFetch } from "../../generated-fetch";

// https://stackoverflow.com/questions/49579094/typescript-conditional-types-filter-out-readonly-properties-pick-only-requir/49579497#49579497
type IfEquals<X, Y, A = X, B = never> =
  (<T>() => T extends X ? 1 : 2) extends <T>() => T extends Y ? 1 : 2 ? A : B;

type WritableKeys<T> = {
  [P in keyof T]-?: IfEquals<
    { [Q in P]: T[P] },
    { -readonly [Q in P]: T[P] },
    P
  >;
}[keyof T];

type UnionToIntersection<U> = (U extends any ? (k: U) => void : never) extends (
  k: infer I,
) => void
  ? I
  : never;
type DistributeReadOnlyOverUnions<T> = T extends any ? NonReadonly<T> : never;

type Writable<T> = Pick<T, WritableKeys<T>>;
type NonReadonly<T> = [T] extends [UnionToIntersection<T>]
  ? {
      [P in keyof Writable<T>]: T[P] extends object
        ? NonReadonly<NonNullable<T[P]>>
        : T[P];
    }
  : DistributeReadOnlyOverUnions<T>;

export const getRecordAgentHookEventUrl = () => {
  return `/api/agent-hook/event`;
};

/**
 * @summary Post API agent hook event
 */
export const recordAgentHookEvent = async (
  request: NonReadonly<Request>,
  options?: RequestInit,
): Promise<AgentHookResponse> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<AgentHookResponse>(getRecordAgentHookEventUrl(), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(request),
  });
};

export const getCompleteAgentHookFixUrl = () => {
  return `/api/agent-hook/fix-done`;
};

/**
 * @summary Post API agent hook fix done
 */
export const completeAgentHookFix = async (
  agentHookFixDoneRequest: NonReadonly<AgentHookFixDoneRequest>,
  options?: RequestInit,
): Promise<AgentHookFixDoneOutputBody> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<AgentHookFixDoneOutputBody>(
    getCompleteAgentHookFixUrl(),
    {
      ...options,
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        ...getHeaders(options?.headers),
      },
      body: JSON.stringify(agentHookFixDoneRequest),
    },
  );
};

export const getResetAgentHookSessionsUrl = () => {
  return `/api/agent-hook/reset`;
};

/**
 * @summary Post API agent hook reset
 */
export const resetAgentHookSessions = async (
  agentHookResetRequest: NonReadonly<AgentHookResetRequest>,
  options?: RequestInit,
): Promise<AgentHookResetOutputBody> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<AgentHookResetOutputBody>(
    getResetAgentHookSessionsUrl(),
    {
      ...options,
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        ...getHeaders(options?.headers),
      },
      body: JSON.stringify(agentHookResetRequest),
    },
  );
};

export const getListAgentHookSessionsUrl = () => {
  return `/api/agent-hook/sessions`;
};

/**
 * @summary Get API agent hook sessions
 */
export const listAgentHookSessions = async (
  options?: RequestInit,
): Promise<AgentHookSessionsOutputBody> => {
  return roborevFetch<AgentHookSessionsOutputBody>(
    getListAgentHookSessionsUrl(),
    {
      ...options,
      method: "GET",
    },
  );
};
