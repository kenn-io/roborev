/**
 * Generated by Orval. Do not edit manually.
 */
import type {
  ActivityOutputBody,
  CostAggregate,
  DaemonStatus,
  GetCostParams,
  GetSummaryParams,
  HealthStatus,
  ListActivityParams,
  PingInfo,
  QueuePauseOutputBody,
  ReleaseNotesResponse,
  ReleaseUpdateOutputBody,
  ShutdownOutputBody,
  StreamEventsParams,
  Summary,
  UpdateDrainRequestBody,
  UpdateDrainStatus,
  UpdateLeaseRequestBody,
} from "../models";

import { roborevFetch } from "../../generated-fetch";

// https://stackoverflow.com/questions/49579094/typescript-conditional-types-filter-out-readonly-properties-pick-only-requir/49579497#49579497
type IfEquals<X, Y, A = X, B = never> =
  (<T>() => T extends X ? 1 : 2) extends <T>() => T extends Y ? 1 : 2 ? A : B;

type WritableKeys<T> = {
  [P in keyof T]-?: IfEquals<
    { [Q in P]: T[P] },
    { -readonly [Q in P]: T[P] },
    P
  >;
}[keyof T];

type UnionToIntersection<U> = (U extends any ? (k: U) => void : never) extends (
  k: infer I,
) => void
  ? I
  : never;
type DistributeReadOnlyOverUnions<T> = T extends any ? NonReadonly<T> : never;

type Writable<T> = Pick<T, WritableKeys<T>>;
type NonReadonly<T> = [T] extends [UnionToIntersection<T>]
  ? {
      [P in keyof Writable<T>]: T[P] extends object
        ? NonReadonly<NonNullable<T[P]>>
        : T[P];
    }
  : DistributeReadOnlyOverUnions<T>;

export const getListActivityUrl = (params?: ListActivityParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/activity?${stringifiedParams}`
    : `/api/activity`;
};

/**
 * @summary List recent daemon activity
 */
export const listActivity = async (
  params?: ListActivityParams,
  options?: RequestInit,
): Promise<ActivityOutputBody> => {
  return roborevFetch<ActivityOutputBody>(getListActivityUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getGetCostUrl = (params?: GetCostParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ["repo"];

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? "null" : String(v));
      });
      return;
    }

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/cost?${stringifiedParams}`
    : `/api/cost`;
};

/**
 * @summary Get aggregate review cost
 */
export const getCost = async (
  params?: GetCostParams,
  options?: RequestInit,
): Promise<CostAggregate> => {
  return roborevFetch<CostAggregate>(getGetCostUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getGetHealthUrl = () => {
  return `/api/health`;
};

/**
 * @summary Get daemon health
 */
export const getHealth = async (
  options?: RequestInit,
): Promise<HealthStatus> => {
  return roborevFetch<HealthStatus>(getGetHealthUrl(), {
    ...options,
    method: "GET",
  });
};

export const getPingUrl = () => {
  return `/api/ping`;
};

/**
 * @summary Get daemon liveness identity
 */
export const ping = async (options?: RequestInit): Promise<PingInfo> => {
  return roborevFetch<PingInfo>(getPingUrl(), {
    ...options,
    method: "GET",
  });
};

export const getPauseQueueUrl = () => {
  return `/api/queue/pause`;
};

/**
 * @summary Pause queue processing
 */
export const pauseQueue = async (
  options?: RequestInit,
): Promise<QueuePauseOutputBody> => {
  return roborevFetch<QueuePauseOutputBody>(getPauseQueueUrl(), {
    ...options,
    method: "POST",
  });
};

export const getUnpauseQueueUrl = () => {
  return `/api/queue/unpause`;
};

/**
 * @summary Resume queue processing
 */
export const unpauseQueue = async (
  options?: RequestInit,
): Promise<QueuePauseOutputBody> => {
  return roborevFetch<QueuePauseOutputBody>(getUnpauseQueueUrl(), {
    ...options,
    method: "POST",
  });
};

export const getListReleasesUrl = () => {
  return `/api/releases`;
};

/**
 * @summary List recent Roborev release notes
 */
export const listReleases = async (
  options?: RequestInit,
): Promise<ReleaseNotesResponse> => {
  return roborevFetch<ReleaseNotesResponse>(getListReleasesUrl(), {
    ...options,
    method: "GET",
  });
};

export const getShutdownUrl = () => {
  return `/api/shutdown`;
};

/**
 * @summary Gracefully shut down the daemon
 */
export const shutdown = async (
  options?: RequestInit,
): Promise<ShutdownOutputBody> => {
  return roborevFetch<ShutdownOutputBody>(getShutdownUrl(), {
    ...options,
    method: "POST",
  });
};

export const getGetStatusUrl = () => {
  return `/api/status`;
};

/**
 * @summary Get daemon status
 */
export const getStatus = async (
  options?: RequestInit,
): Promise<DaemonStatus> => {
  return roborevFetch<DaemonStatus>(getGetStatusUrl(), {
    ...options,
    method: "GET",
  });
};

export const getStreamEventsUrl = (params?: StreamEventsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/stream/events?${stringifiedParams}`
    : `/api/stream/events`;
};

/**
 * @summary Stream daemon events
 */
export const streamEvents = async (
  params?: StreamEventsParams,
  options?: RequestInit,
): Promise<void> => {
  return roborevFetch<void>(getStreamEventsUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getGetSummaryUrl = (params?: GetSummaryParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/summary?${stringifiedParams}`
    : `/api/summary`;
};

/**
 * @summary Get review summary statistics
 */
export const getSummary = async (
  params?: GetSummaryParams,
  options?: RequestInit,
): Promise<Summary> => {
  return roborevFetch<Summary>(getGetSummaryUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getPrepareUpdateUrl = () => {
  return `/api/update/prepare`;
};

/**
 * @summary Prepare a leased update drain
 */
export const prepareUpdate = async (
  updateDrainRequestBody: NonReadonly<UpdateDrainRequestBody>,
  options?: RequestInit,
): Promise<UpdateDrainStatus> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<UpdateDrainStatus>(getPrepareUpdateUrl(), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(updateDrainRequestBody),
  });
};

export const getReleaseUpdateUrl = () => {
  return `/api/update/release`;
};

/**
 * @summary Release an update drain lease
 */
export const releaseUpdate = async (
  updateLeaseRequestBody: NonReadonly<UpdateLeaseRequestBody>,
  options?: RequestInit,
): Promise<ReleaseUpdateOutputBody> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<ReleaseUpdateOutputBody>(getReleaseUpdateUrl(), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(updateLeaseRequestBody),
  });
};

export const getRenewUpdateUrl = () => {
  return `/api/update/renew`;
};

/**
 * @summary Renew an update drain lease
 */
export const renewUpdate = async (
  updateLeaseRequestBody: NonReadonly<UpdateLeaseRequestBody>,
  options?: RequestInit,
): Promise<UpdateDrainStatus> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Array.isArray(h)) return Object.fromEntries(h);
    return h;
  };
  return roborevFetch<UpdateDrainStatus>(getRenewUpdateUrl(), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(updateLeaseRequestBody),
  });
};
